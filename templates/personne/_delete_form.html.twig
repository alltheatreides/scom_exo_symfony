<form method="post" action="{{ path('app_personne_delete', {'idpersonne': personne.idpersonne}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ personne.idpersonne) }}">
    <button class="btn">Delete</button>
</form>
